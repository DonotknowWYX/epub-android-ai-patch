name: Android Build & Release

on:
  push:
    branches: [master]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [11]  # 统一使用 JDK 11
        gradle: [7.5.1]  # 与 AGP 7.4.2 兼容

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史记录

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'

      - name: Cache Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: "9.0"
          api-level: 34  # 最低支持 API 21

      - name: Install Build Tools
        run: |
          yes | sdkmanager "build-tools;34.0.0" "platforms;android-34"

      - name: Configure Project
        run: |
          echo "sdk.dir=${ANDROID_HOME}" > local.properties
          echo "org.gradle.daemon=false" >> gradle.properties  # 禁用守护进程
          echo "org.gradle.parallel=false" >> gradle.properties  # 禁用并行构建
          echo "android.injected.build.model.only.versioned=3" >> gradle.properties

      - name: Clean Build
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf app/build/
          chmod +x gradlew
          ./gradlew clean --refresh-dependencies

      - name: Build Release APK
        env:
          GRADLE_OPTS: "-Xmx4g -Dfile.encoding=UTF-8"
        run: ./gradlew assembleRelease --stacktrace --info

      - name: Sign APK (Optional)
        if: github.ref == 'refs/heads/master'  # 仅在主分支构建时签名
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Release-APK
          path: |
            app/build/outputs/apk/release/*.apk
            build/reports/gradle-logs/

  publish:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'  # 仅在主分支发布

    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: Release-APK

      - name: Publish to GitHub Release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: "v$(date +%s)"  # 自动生成时间戳版本号
          name: "Release Build $(date +%Y-%m-%d)"
          body: "Automated build from master branch"
          draft: false
          prerelease: false
          artifacts: "*.apk"
